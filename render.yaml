services:
  - type: worker
    name: ats-resume-analyzer-bot
    runtime: node
    buildCommand: npm install
    startCommand: node bot.js
    plan: free
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: NODE_ENV
        value: production
